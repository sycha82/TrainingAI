# 물류센터 설비 AI 매칭 엔진 - 필수 패키지
# Python 3.7 이상 필요

# 현재는 표준 라이브러리만 사용하므로 외부 패키지 불필요
# 향후 확장을 위한 권장 패키지들:

# numpy>=1.21.0          # 수치 계산 (향후 머신러닝 확장 시)
# pandas>=1.3.0          # 데이터 분석 (대량 데이터 처리 시)
# scikit-learn>=1.0.0    # 머신러닝 (향후 AI 모델 학습 시)
